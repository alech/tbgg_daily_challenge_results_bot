{
  "name": "tbgg-bot-infra",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "lockdeps": "uv export --frozen --no-dev --no-emit-project --format requirements-txt -o ../requirements-lambda.txt --project .."
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "aws-cdk": "^2.1141.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.269.0",
    "constructs": "^10.8.1"
  }
}
